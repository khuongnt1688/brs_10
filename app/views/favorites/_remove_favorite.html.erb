<%= form_for  [current_user, current_user.favorites.find_by(book: @book)], 
              html: {method: :delete}, remote: true do |f| %>
  <%= f.submit "Remove from favorite", class: "btn btn-danger" %>
<% end %>